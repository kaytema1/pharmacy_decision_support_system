<%= simple_form_for(@prescription) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :patient_visit %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
